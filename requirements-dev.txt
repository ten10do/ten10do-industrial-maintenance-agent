-r requirements.txt

# OpenTelemetry. Tracing is optional at runtime, but the tests exercise the real
# SDK, so the optional set is a development dependency. That keeps the assertion
# "tracing is inert when disabled" honest: the packages are present and the code
# still imports and calls nothing.
-r requirements-otel.txt

# --- Testing ---
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<7.0

# --- Tooling ---
ruff>=0.5,<0.9
mypy>=1.10,<2.0

# --- Test-only parsing ---
# Used by tests/test_docker_config.py to read docker-compose.yml structurally.
# It already arrives transitively through uvicorn[standard]; declared here so the
# Docker configuration tests do not depend on that transitive path. The stub
# package is what mypy needs, since PyYAML ships no inline annotations.
pyyaml>=6.0,<7.0
types-PyYAML>=6.0,<7.0
